#all cache keys should be registered here
class CacheKeysEnum():
    """This class functions like a enum here will be registered all app cache keys..."""
    bearerToken = 'bearerToken'  # Bearer token
    userInfo = 'userInfo' #info user in session
    userInSession = 'userInSession'#all data on cache